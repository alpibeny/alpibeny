import { createApp } from 'vue'
import './style.css'
// Импортируем локальный шрифт скругленных иконок из node_modules
import 'material-symbols/rounded.css'
import App from './App.vue'

createApp(App).mount('#app')
